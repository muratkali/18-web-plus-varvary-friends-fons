
    // let xStart = null
    // let blocked = false

    // export function handleTouchStart(event) {
    //     const firstTouch = event.touches[0]
    //     xStart = firstTouch.clientX
    // }

    // export function handleTouchMove(event) {
    //     if (blocked) return
    //     const xMove = event.touches[0].clientX
    //     const xDiff = xMove - xStart
    //     const participantDirection = xDiff > 0 ? 'previous' : 'next'
    //     blocked = true
    //     changeParticipant(participantDirection)
    //     setTimeout(() => blocked = false, 300)
    // };

